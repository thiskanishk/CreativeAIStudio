// Export the API services
export { default as axios } from './axios';
export { default as adService } from './services/adService';

// Add more service exports as needed
// This improves TypeScript module resolution and allows for cleaner imports 